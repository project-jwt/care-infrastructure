// TODO: adapters for spec §Auth.
// Functions to export:
//   - register({ email, password, fullName, role })  -> POST /api/auth/register
//   - login({ email, password })                     -> POST /api/auth/login
//   - logout()                                       -> clears the JWT from localStorage
