// TODO: speech-to-text capture + clarifying-question loop (spec §MVP 2).
// Calls draftSummary() until needsClarification === false, then hands off to SummaryReview.

export default function RecordingPage() {
  return <div>TODO: RecordingPage</div>;
}
