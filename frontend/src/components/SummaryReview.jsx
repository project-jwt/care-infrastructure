// TODO: editable summary review before/after sending (spec §MVP 3).
// On "looks good", call saveSummary() then advance to ChooseAction.

export default function SummaryReview() {
  return <div>TODO: SummaryReview</div>;
}
