// TODO: role picker + login + register forms (spec §MVP 1).
// On success, store the JWT in localStorage and lift the user object to App.

export default function LoginRegisterPage() {
  return <div>TODO: LoginRegisterPage</div>;
}
