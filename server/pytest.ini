[pytest]
# asyncio_mode = auto: plain `async def test_*` and async fixtures run without
# per-item decorators. pythonpath = . puts server/ on sys.path so the app's
# absolute imports (config, db, models, ...) resolve however pytest is invoked.
asyncio_mode = auto
pythonpath = .
testpaths = tests
