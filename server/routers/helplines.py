# TODO: implement per spec §Helplines
#   - GET /api/helplines -> list of pre-loaded helplines (id, name, phone, hours, description)

from fastapi import APIRouter

# router = APIRouter(prefix="/helplines", tags=["helplines"])
