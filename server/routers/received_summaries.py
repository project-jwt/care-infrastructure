# TODO: implement per spec §Contact Dashboard (Contact only)
#   - GET /api/received-summaries -> summaries sent to the current contact, newest first

from fastapi import APIRouter

# router = APIRouter(prefix="/received-summaries", tags=["received-summaries"])
